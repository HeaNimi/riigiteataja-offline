services:
  riigiteataja:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      RT_DATA_DIR: /data
    volumes:
      - ./data:/data
    restart: unless-stopped
